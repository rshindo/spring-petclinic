version: 2.1

orbs:
  maven: cimg/openjdk:11.0

jobs:
  build:
    docker:
      - image: cimg/openjdk:11.0
    steps:
      - checkout
      - run:
        name: test
        command: ./mvnw test
      - run:
        name: package
        command: ./mvnw  package -DskipTests

  
